[buildout]
parts = python django
develop = .
eggs = musicquiz

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}

[django]
recipe = djangorecipe
version = 1.1.1
project = musicquiz
projectegg = musicquiz
test = quiz
settings = settings
eggs = ${buildout:eggs}
